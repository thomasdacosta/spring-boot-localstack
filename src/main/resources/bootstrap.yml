spring:
  application:
    name: spring-boot-localstack
---
spring:
  config:
    activate:
      on-profile: production
---
spring:
  config:
    activate:
      on-profile: localstack
cloud:
  aws:
    sqs:
      endpoint: http://localhost:4566
    sns:
      endpoint: http://localhost:4566
